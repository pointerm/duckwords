// Package logging constructs DuckWords' structured stderr logger and records the
// first underlying writer failure that log/slog intentionally does not return from
// Logger methods.
package logging
